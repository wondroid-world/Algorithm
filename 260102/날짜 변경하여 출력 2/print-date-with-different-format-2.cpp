#include <iostream>
using namespace std;

int main() {
    int mm, dd, yyyy;

    cin >> mm;
    cin.get();
    cin >> dd;
    cin.get();
    cin >> yyyy;

    cout << yyyy << "." << mm << "." << dd;

    return 0;
}